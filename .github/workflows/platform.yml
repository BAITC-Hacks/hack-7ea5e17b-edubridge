name: Platform checks
on:
  push:
    branches: [feat/agent-platform, feat/forecast-model]
  pull_request:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - run: python -m pip install -e '.[dev,model]' -r ui/requirements.txt
      - run: python -m pytest -q
      - run: python -m ruff check src/wind_agent tests/model tests/weather scripts/validate_model_package.py
      - run: python -m wind_agent --config config/demo.json replay --horizon-hours 48
