# Replay exports are hashed evidence: keep identical bytes on every platform.
outputs/forecast/*.json -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
outputs/forecast/*.csv -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
